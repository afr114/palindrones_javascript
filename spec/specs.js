describe("palindromeCheck", function() {
  // it("checks to see if a word is a palindrome", function() {
  //   expect(palindromeCheck("mom")).to.equal(true);
  // });
  //
  // it("checks to see if a phrase is a palindrome", function(){
  //   expect(palindromeCheck("lion oil")).to.equal(true);
  // });

  it("checks to see if a number is a palindrome", function(){
    expect(palindromeCheck(10001)).to.equal(true)
  });
});
